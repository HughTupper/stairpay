{
  "name": "@stairpay/database",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "db:start": "npx supabase start",
    "db:stop": "npx supabase stop",
    "db:reset": "npx supabase db reset",
    "db:push": "npx supabase db push",
    "db:pull": "npx supabase db pull",
    "db:migration": "npx supabase migration new",
    "db:types": "mkdir -p types && npx supabase gen types typescript --local > types/database.ts",
    "db:types:remote": "mkdir -p types && npx supabase gen types typescript --project-id rnsfmlijruywbaxldpvk > types/database.ts",
    "db:seed": "npx tsx seed.ts",
    "build": "mkdir -p types && touch types/database.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.86.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "supabase": "^1.200.3",
    "tsx": "^4.19.2",
    "typescript": "^5"
  }
}
