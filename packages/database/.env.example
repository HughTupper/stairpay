# Environment Variables for Database Tooling
# Copy this file to create environment-specific configurations
# All variables are validated by T3 Env at runtime

# ============================================================================
# Supabase Configuration (Required)
# ============================================================================

# For local development:
# Run `npx supabase status` after `npm run db:start` to get these values
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY=your-local-service-role-key-here

# For production:
# Get these from Supabase Dashboard → Project Settings → API
# Project: rnsfmlijruywbaxldpvk
# SUPABASE_URL=https://rnsfmlijruywbaxldpvk.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your-remote-service-role-key-here

# ============================================================================
# Environment Files
# ============================================================================
# .env.local - Local development (gitignored)
# .env.production - Production environment (gitignored)

# ============================================================================
# Usage
# ============================================================================
# npm run db:seed              - Uses .env.local
# npm run db:seed:production      - Uses .env.production
