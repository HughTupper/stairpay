# Environment Variables for Database Tooling
# Copy this file to .env.local and fill in the values
# All variables are validated by T3 Env at runtime

# ============================================================================
# Supabase Configuration for Database Seeding (Required)
# ============================================================================
# These variables are used by scripts that need direct database access
# via the Supabase service role (bypasses RLS)

# For local development with `supabase start`:
# Run `npx supabase status` to get these values
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# For production:
# Get these from your Supabase project settings > API
# WARNING: Service role key has admin access - keep it secret!
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your-production-service-role-key

# ============================================================================
# Usage
# ============================================================================
# The seed script validates these vars using T3 Env
# If vars are missing or invalid, you'll get clear error messages
# Run: npm run seed (from packages/database directory)
