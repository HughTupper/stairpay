# Environment Variables for Housing Association CRM
# Copy this file to .env.local and fill in the values
# All variables are validated by T3 Env at build time and runtime

# ============================================================================
# Supabase Configuration (Required)
# ============================================================================
# Get these from your Supabase project settings
# For local development: Run `npx supabase status` to get local values

# Supabase Project URL
# Format: https://your-project.supabase.co (production)
#         http://127.0.0.1:54321 (local development)
NEXT_PUBLIC_SUPABASE_URL=your-project-url

# Supabase Publishable Key (safe to expose to browser)
# This is the "anon" key in your Supabase project settings
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-key

# ============================================================================
# Application Configuration (Auto-detected)
# ============================================================================
# NODE_ENV is automatically set by Next.js
# development: Local dev server (npm run dev)
# production: Production builds (npm run build)
# test: Testing environment
# NODE_ENV=development

# ============================================================================
# Optional: Supabase Service Role Key (Server-only)
# ============================================================================
# Only needed for server-side admin operations
# NEVER expose this to the client - it bypasses Row Level Security
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ============================================================================
# CI/CD Configuration
# ============================================================================
# Set SKIP_ENV_VALIDATION=true in CI if env vars are provided at runtime
# Generally should be false to catch missing vars early
# SKIP_ENV_VALIDATION=false
